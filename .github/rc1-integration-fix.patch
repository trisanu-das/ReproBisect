diff --git a/scripts/test-fixtures.sh b/scripts/test-fixtures.sh
--- a/scripts/test-fixtures.sh
+++ b/scripts/test-fixtures.sh
@@ -4,7 +4,7 @@ ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
 BIN="${ROOT}/target/debug/reprobisect"
 
-cargo build --manifest-path "${ROOT}/Cargo.toml"
+cargo build --locked --manifest-path "${ROOT}/Cargo.toml"
 
 cleanup_fixture() {
   rm -rf "${ROOT}/fixtures/$1/.reprobisect"
diff --git a/scripts/test-podman.sh b/scripts/test-podman.sh
--- a/scripts/test-podman.sh
+++ b/scripts/test-podman.sh
@@ -17,7 +17,7 @@ path.write_text(text)
 output="$tmp/report.json"
 (
   cd "$repo_root"
-  cargo run --quiet -- check "$tmp" --format json > "$output"
+  cargo run --locked --quiet -- check "$tmp" --format json > "$output"
 )
 python3 - "$output" <<'PY'
 import json, sys
diff --git a/scripts/release-check.py b/scripts/release-check.py
--- a/scripts/release-check.py
+++ b/scripts/release-check.py
@@ -63,6 +63,16 @@ def check_package() -> list[str]:
     if toolchain.get("toolchain", {}).get("channel") != EXPECTED_TOOLCHAIN:
         raise RuntimeError(f"rust-toolchain.toml must pin {EXPECTED_TOOLCHAIN}")
+
+    lock_path = ROOT / "Cargo.lock"
+    if not lock_path.is_file():
+        raise RuntimeError("Cargo.lock must be committed for the stable release line")
+    lock = tomllib.loads(lock_path.read_text(encoding="utf-8"))
+    root_packages = [
+        package for package in lock.get("package", []) if package.get("name") == "reprobisect"
+    ]
+    if len(root_packages) != 1 or root_packages[0].get("version") != EXPECTED_VERSION:
+        raise RuntimeError(f"Cargo.lock must contain reprobisect {EXPECTED_VERSION}")
+    notes.append("Cargo.lock is committed and matches the package version")
     notes.append(f"package={EXPECTED_VERSION}; msrv={EXPECTED_RUST_VERSION}; default_toolchain={EXPECTED_TOOLCHAIN}")
     return notes
@@ -130,8 +140,8 @@ def rust_checks(require_rust: bool) -> tuple[list[str], list[dict[str, Any]]]:
     commands = [
         run([rustc, "--version"]),
         run([cargo, "--version"]),
-        run([cargo, "check", "--all-targets"]),
-        run([cargo, "test", "--all-targets"]),
+        run([cargo, "check", "--locked", "--all-targets"]),
+        run([cargo, "test", "--locked", "--all-targets"]),
     ]
     return ["Rust compiler and all-target unit gates passed"], commands
